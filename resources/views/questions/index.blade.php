@extends('layouts.app') @section('title','Bank Soal') @section('content')
<div class="flex flex-wrap items-center justify-between gap-4"><div><h2 class="text-3xl font-black">Bank Soal</h2><p class="text-slate-500">Kelola soal matematika, fisika, dan bidang lainnya.</p></div><a href="{{route('questions.create')}}" class="btn-action btn-primary-solid rounded-2xl px-5">+ Buat Soal</a></div>
<form class="mt-6 grid gap-3 rounded-3xl bg-white p-5 shadow-sm md:grid-cols-4">@foreach(['subject_id'=>'Subtest','type'=>'Jenis','difficulty'=>'Kesulitan','status'=>'Status'] as $key=>$label)<select name="{{$key}}" class="rounded-xl border-slate-200"><option value="">{{$label}}: Semua</option>@if($key==='subject_id')@foreach($subjects as $s)<option value="{{$s->id}}" @selected(request($key)==$s->id)>{{$s->name}}</option>@endforeach @elseif($key==='type')<option value="multiple_choice">Pilihan Ganda</option><option value="essay">Esai</option>@elseif($key==='difficulty')<option value="easy">Mudah</option><option value="medium">Sedang</option><option value="hard">Sulit</option>@else<option value="active">Aktif</option><option value="inactive">Nonaktif</option>@endif</select>@endforeach<button class="rounded-xl bg-slate-900 px-4 py-2 font-bold text-white">Terapkan Filter</button></form>
<div class="mt-5 overflow-x-auto rounded-3xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="p-4">Soal</th><th>Mapel / Materi</th><th>Jenis</th><th>Bobot</th><th>Aksi</th></tr></thead><tbody class="divide-y">@forelse($questions as $q)<tr><td class="max-w-xl p-4 font-semibold"><span class="math-content">{!! strip_tags($q->question,'<p><br><strong><em>') !!}</span><div class="mt-1 text-xs text-slate-400">{{$q->difficulty}} · {{$q->class_level ?: 'Semua kelas'}}</div></td><td>{{$q->subject->name}}<br><span class="text-xs text-slate-400">{{$q->topic?->name}}</span></td><td>{{$q->type==='essay'?'Esai':'Pilihan Ganda'}}</td><td>{{$q->score}}</td><td><div class="flex gap-2"><a class="font-bold text-indigo-600" href="{{route('questions.edit',$q)}}">Edit</a><form method="post" action="{{route('questions.duplicate',$q)}}">@csrf<button class="font-bold text-emerald-600">Duplikat</button></form><form method="post" action="{{route('questions.destroy',$q)}}" onsubmit="return confirm('Hapus soal?')">@csrf @method('DELETE')<button class="font-bold text-rose-600">Hapus</button></form></div></td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-500">Belum ada soal.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{$questions->links()}}</div>
@include('questions.math') @endsection
